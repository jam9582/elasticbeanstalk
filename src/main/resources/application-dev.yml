server:
  # 엘라스틱빈즈톡으로 배포를 하게 되면 nginx에 의해 5000번 default로 프록시 처리가 된다.
  port: 5000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/menudb # rds 주소 복사
    username: ohgiraffers
    password: ohgiraffers

  jpa:
    generate-ddl: false
    show-sql: true
    database: mysql
    properties:
      hibernate:
        format_sql: true